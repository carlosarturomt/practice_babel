{
  "name": "babel",
  "version": "1.0.0",
  "description": "project to exercices of babel's course",
  "main": "index.js",
  "scripts": {
    "start": "wds --node-resolve --watch --open",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "carlosarturomt",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6"
  }
}
